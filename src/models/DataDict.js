import BaseModel from '../cores/model/BaseModel'

class DataDict extends BaseModel {
    constructor(data = {}) {
        super(data)

        this.format = {
        }

        this.rules = [

        ]
    }
}

export default DataDict
